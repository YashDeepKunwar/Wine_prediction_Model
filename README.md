# Wine_prediction_Model
Wine quality classifier (Red Wine dataset) — Random Forest tuned via GridSearchCV, with a documented investigation into class imbalance, outlier-removal bias, and the accuracy-vs-recall tradeoff.
